### RB-000  -  Cost and Safety Basics ###

## Purpose
- Prevent suprise costs, lost work, and unsafe actions during labs/projects

## Always-on safety rules (daily)
1) Verify location before creating/deleting: `pwd` then `ls`
2) For destructive actions, prefer safe prompts first (ex: -i)
3) Commit + push at least once per day
4) Take VM Snapshots before risky changes
5) End every lab with cleanup

## VM safety
- proper shutdown method:
- Snapshot naming convention:
- When to Snapshot:

## Git safety
- How to check current status: `git status`
- Daily commit rule
- Commit message format

## AWS cost safety
- Root account MFA date
- Budget/alert date
- Teardown checklist after every lab:
   1)
   2)
   3)
- How i verify teardown (evidence)
